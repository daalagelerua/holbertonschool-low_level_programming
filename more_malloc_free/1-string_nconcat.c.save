#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
*
*
*
*
*
*/

char *string_nconcat(char *s1, char *s2, unsigned int n)
{
char str;
int i, j;

if (s1 == NULL)
	s1 = "";
if (s2 == NULL)
	s2 = "";

for (i = 0; s1[i] != '\0'; i++)

for (j = 0; s2[j] <= n; j++)

if (n >= s2)

if (str == NULL)
	return (NULL);

}

